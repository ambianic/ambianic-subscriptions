{
  "name": "functions",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "parcel build index.html",
    "dev": "parcel index.html",
    "generate-swagger": "openapi-generator-cli generate -i docs/*.yaml -g javascript -o docs",
    "netlify-dev": "cd netlify && netlify dev --port=5050"
  },
  "license": "MIT",
  "dependencies": {
    "@openapitools/openapi-generator-cli": "^2.1.26",
    "netlify-cli": "^3.13.10",
    "newman": "^5.2.2",
    "stripe": "^8.135.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
